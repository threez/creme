/* (creme bigdecimal) — arbitrary-precision exact decimal arithmetic.
 * See bigdecimal.c's own header comment. */
#ifndef CREME_BIGDECIMAL_H
#define CREME_BIGDECIMAL_H

#include "vm.h"

void creme_register_bigdecimal_builtins(VM *vm);

#endif
