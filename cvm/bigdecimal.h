/* (creme bigdecimal) — arbitrary-precision exact decimal arithmetic.
 * See bigdecimal.c's own header comment. */
#ifndef CVM_BIGDECIMAL_H
#define CVM_BIGDECIMAL_H

#include "vm.h"

void cvm_register_bigdecimal_builtins(VM *vm);

#endif
